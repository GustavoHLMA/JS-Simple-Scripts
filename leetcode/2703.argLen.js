const argumentsLength = (...args) => {
  return args.length
}

//when '...' is used at function arguments, it is called rest parameter and it will be an array with all arguments passed to the function